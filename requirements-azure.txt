# Dependencias da Azure Function (jobs agendados: sync incremental do RD CRM +
# reprocessamento de leads + lembretes de reuniao). NAO e o requirements.txt
# da raiz porque o remote build do Azure Flex Consumption tem timeout de 60s
# pra instalar tudo -- o requirements.txt completo (com fastapi/uvicorn do
# webhook e streamlit/plotly/pandas do dashboard, que a Function nao usa)
# estourava esse limite. Ver .github/workflows/main_func-dashboard-api-prod.yml
# (troca o requirements.txt por este, so na copia zipada, antes do deploy) e
# a docstring de function_app.py.
#
# Confirmado por grep (2026-09-16): nenhum modulo em ingestion/, scripts/,
# database/ ou config/ importa pandas, streamlit, plotly, fastapi ou uvicorn
# -- esses so aparecem em app/ (Streamlit) e api/ (FastAPI do Render), que
# nao fazem parte deste deploy.
#
# "requests" nao e importado direto por nenhum modulo nosso, mas
# google.auth.transport.requests (usado por ingestion/ga4/client.py) precisa
# dele -- no requirements.txt da raiz isso vinha de carona via alguma outra
# lib (streamlit/pandas), aqui precisa estar explicito.
#
# "tzdata" garante a base de fusos horarios IANA (zoneinfo, usado com
# "America/Sao_Paulo" nos lembretes) independente do SO/imagem ter isso
# instalado no sistema -- confirmado que falta em ambiente Windows local,
# entao nao da pra confiar que a imagem do Azure Linux tem por padrao.

azure-functions==1.25.0
httpx==0.27.2
SQLAlchemy==2.0.35
psycopg2-binary>=2.9.10
pydantic-settings==2.5.2
python-dotenv==1.0.1
tenacity==9.0.0
google-auth==2.35.0
requests==2.32.3
anthropic==1.4.0
tzdata==2025.2
