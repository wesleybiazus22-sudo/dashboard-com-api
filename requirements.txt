# Dependencias unicas do repositorio. O Streamlit Community Cloud instala
# obrigatoriamente o requirements.txt da RAIZ -- por isso as libs do dashboard
# (streamlit/plotly/pandas) vivem aqui junto com as da API, em vez de num arquivo
# separado que o Cloud simplesmente ignoraria.
#
# NAO adicionar "azure-functions" aqui -- ja quebrou o deploy do Streamlit Cloud
# uma vez (2026-09-14). A Azure Function (function_app.py) tambem precisa de um
# requirements.txt no mesmo diretorio do host.json pra funcionar, o que colide
# com essa mesma restricao do Streamlit -- ainda sem solucao definitiva (ver
# docstring de function_app.py). Resolver quando o Function App for criado de
# verdade no Azure, testando contra o deploy real em vez de adivinhar.

# --- API + ingestao (Render, Azure Functions) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
SQLAlchemy==2.0.35
psycopg2-binary>=2.9.10
pydantic-settings==2.5.2
python-dotenv==1.0.1
tenacity==9.0.0
google-auth==2.35.0
anthropic==1.4.0

# --- Dashboard (Streamlit Community Cloud) ---
streamlit==1.39.0
plotly==5.24.1
pandas==2.2.3
