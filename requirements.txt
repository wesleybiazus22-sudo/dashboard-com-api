# Dependencias unicas do repositorio. O Streamlit Community Cloud instala
# obrigatoriamente o requirements.txt da RAIZ -- por isso as libs do dashboard
# (streamlit/plotly/pandas) vivem aqui junto com as da API, em vez de num arquivo
# separado que o Cloud simplesmente ignoraria.
#
# NAO adicionar "azure-functions" aqui -- ja quebrou o deploy do Streamlit Cloud
# uma vez (2026-09-14), e o remote build da Azure Function tem timeout de 60s
# que as libs do dashboard (streamlit/plotly/pandas) e do webhook
# (fastapi/uvicorn) estourariam de qualquer forma. A Azure Function usa
# requirements-azure.txt (dependencias minimas + azure-functions) -- o
# workflow .github/workflows/main_func-dashboard-api-prod.yml troca por ele
# SO na copia zipada, no runner do CI, sem tocar neste arquivo. Ver docstring
# de function_app.py.

# --- API + ingestao (Render, Azure Functions) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
SQLAlchemy==2.0.35
psycopg2-binary>=2.9.10
pydantic-settings==2.5.2
python-dotenv==1.0.1
tenacity==9.0.0
google-auth==2.35.0
anthropic==1.4.0

# --- Dashboard (Streamlit Community Cloud) ---
streamlit==1.39.0
plotly==5.24.1
pandas==2.2.3
