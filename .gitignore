.env
# Projeto separado do usuario que vive dentro desta mesma pasta -- nao pertence
# ao repositorio da API (tem inclusive seu proprio repositorio git quebrado dentro).
Dashboard Acompanhamento Marketing/
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/
.streamlit/secrets.toml
*.log

# Azure Functions -- segredos locais (nunca comitar), pasta de build/publish.
local.settings.json
.python_packages/
bin/
obj/

# Scripts descartaveis de exploracao/diagnostico -- nao fazem parte do projeto.
.tmp_explore/
